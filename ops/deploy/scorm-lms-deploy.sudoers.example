# Validate with visudo before installing as /etc/sudoers.d/scorm-lms-deploy.
# Replace "deploy" if the SSH deployment user has another name.
deploy ALL=(root) NOPASSWD: /usr/local/sbin/scorm-lms-deploy *

