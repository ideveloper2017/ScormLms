/**
 * Test React Query Hooks
 * 
 * This module provides React Query hooks for managing test-related operations.
 * It handles test listing, test taking workflow, answer submission, and results viewing.
 * 
 * @module hooks/tests
 */

export {
  useTests,
  useTest,
  useStartTest,
  useSubmitAnswer,
  useSubmitTest,
  useTestResults,
  useTestHistory,
} from './useTests';
