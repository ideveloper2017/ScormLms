spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/scorm_lms
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  jwt:
    secret: your-256-bit-secret-key-here-1234567890
    expiration-ms: 86400000  # 24 hours

server:
  port: 8080
